const NotFoundPage = () => {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-zinc-900 p-4 text-4xl font-medium text-white">
      (404) The content you are looking for does not exist...
    </div>
  );
};

export default NotFoundPage;
